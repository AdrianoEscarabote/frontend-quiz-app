import { Meta, StoryObj } from "@storybook/react";
import QuizCompleted from ".";

export default {
  title: "components/QuizCompleted",
  component: QuizCompleted,
} as Meta;

export const Primary: StoryObj = {};
