import { Meta, StoryObj } from "@storybook/react";
import QuestionDetails from ".";

export default {
  title: "components/QuestionDetails",
  component: QuestionDetails,
} as Meta;

export const Primary: StoryObj = {};
